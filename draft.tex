\documentclass{article}
\title{Modelling the Efficiency of a Sheep Pasture in Minecraft}
\author{Evan Freeland}
\usepackage{graphicx}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
valarante child game.... look to cartoon grapfix to make kid player happy like children show.. valarante cartoon world with rainbow unlike counter strike with dark corridorr and raelistic gun.. valarante like playhouse. valarant playor run from csgo fear of dark world and realism
\end{abstract}

\section{Introduction}

\textit{Minecraft} is an open-world sandbox video game\footnote{This investigation was conducted in Minecraft version 1.19.4.}. The game takes place in a procedurally generated world made of cubic voxels, or \textit{blocks}. Among other activities, players can engage in agriculture, including the raising of sheep. Sheep can be sheared sheared to gather wool, and require grass to eat to regrow their wool after shearing. Grass blocks are converted to dirt after being eaten, and may be converted back to grass by other adjacent grass blocks. If a pasture is packed too densely with sheep, they will deplete the grass, and wool production will be low. However, if the pasture is not densely packed, wool production will still be low because there are not many sheep. Between these two extremes, there is an optimal sheep population that will maximize wool output.

\subsection{Deterministic vs. Stochastic}

\subsection{A brief tangent about units}
Canonically, one block is one cubic meter. 
\section{Key assumptions and notes}

There are a couple key assumptions being made here:
\begin{itemize}
\item Grass growth is a simple process in which all dirt blocks have an equal chance to turn into a grass block on a tick. This is not entirely realistic, since grass growth is contingent on spreading from an adjacent block, but as long as dirt blocks consistently have adjacent grass blocks it should work.
\item The actual rate at which the farm produces wool is limited by the rate at which the farmer shears the sheep. Initially, we will assume the farmer is infinitely fast, so really the goal is to have the greatest rate of sheep eating grass. Later, we will look at the mechanics of a farm where the farmer harvests the wool at regular intervals.
\item Another important thing to note is that pastures can be discussed in terms of \textit{blocks} or \textit{area}. One block is canonically one square meter, So pasture size has units of area. However, area in minecraft is discretized, i.e. you cannot have fractional area. For this reason, pasture area will be discussed using the unitless quantity \textit{blocks}.
\end{itemize}

\section{Model}
\subsection{Grass consumption by sheep}
Sheep attempt to eat at a rate $R$ [s\textsuperscript{-1}]. A sheep's attempt to eat will only succeed if it is standing on a grass block. A pasture containing $N$ sheep, consisting of $A$ blocks, $N_g$ of which are grass, will lose grass at a rate

\begin{equation}
    \label{sheep_eat}
    \frac{NRN_g}{A}
\end{equation}

as it is eaten by sheep. This assumes that the sheep are distributed uniformly so that the probability of any sheep standing on a grass block is equal to the grass cover of the pasture.

\subsection{Grass growth}
The rate at which grass grows not as simple. If $N_g$ is small, then grass growth will be limited by the lack of places the grass can spread \textit{from}. If $N_g$ is large, grass growth will be limited by a lack of places for the grass to spread \textit{to}. We will employ a logistic model, where population follows the differential equation:

$$\dot{y}=ky\left(1-\frac{y}{p}\right)$$

Where $y(t)$ is the population at time $t$, $k$ is a parameter representing the reproduction rate of the population and $p$ is the carrying capacity, the maximum population that the local environment can sustain.

The logistic model is one of the oldest models for population growth. It was born from the older exponential (or \textit{Malthusian}) model, but modified to include a maximum population size. It is useful as a tool of instruction, but it's not very useful for modelling actual populations, and has since been supplanted by more sophisticated models. However, we are not dealing with an actual population, but rather a video game which has simple population mechanics. Therefore the model is appropriate for our purposes. For our purposes, the population of interest is the number of grass blocks in the pasture $N_g$, and the carrying capacity is $A$:

\begin{equation}
    \label{grass_regrow}
      \dot{N}_g=kN_g\left(1-\frac{N_g}{A}\right)  
    \end{equation}

Where k [s\textsuperscript{-1}] is a constant as described above.

\subsection{The fast farmer}

If the pasture reaches equilibrium, the rate at which the sheep eat should be equal to the rate at which the grass is regrowing. We can express this, by combining (\ref{sheep_eat}) and (\ref{grass_regrow}), as

$$\frac{NRN_g}{A}=\dot{N}_g=kN_g\left(1-\frac{N_g}{A}\right)$$

This expression is important because it gives us the equilibrium grass cover as a function of sheep population - a little algebra gives 

\begin{equation}
    \label{grass_cover}
    N_g=A-\frac{NR}{k}
\end{equation}

Plugging this into our logistic model for grass growth (\ref{grass_regrow}) gives

$$\dot{N}_g = NR - \frac{N^2R^2}{Ak}$$

This is the steady-state grass regrowth rate as a function of sheep population. If the farmer is shearing sheep as quickly as they get their wool, they are getting 1 block of wool for every block of grass the sheep eat. Let's define $F$ [s\textsuperscript{-1}] as the rate at which the farmer gathers wool.

\begin{equation}
\label{ffast}
F_{fast}=\dot{N}_g=NR-\frac{N^2R^2}{Ak}
\end{equation}

We can find the maximum by differentiating (\ref{ffast}) with respect to $N$, setting the derivative to 0 and solving for $N$:

$$\frac{dF_{fast}}{dN}=R-\frac{2NR^2}{Ak}=0$$

\begin{equation}
    \label{optimal_n_ffast}
    N=\frac{Ak}{2R}
\end{equation}

According to the assumptions we've made, this is the sheep population that will produce the maximum rate of grass growth. At this population,

$${F_{fast}=\frac{Ak}{4}}$$

\subsection{The realistically slow farmer}

For the case of the infinitely fast farmer, the rate at which wool is harvested is the same as the rate at which the sheep are eating. To account for the farmer being slow, we'll need to think a bit more.

The farmer is off doing other things, and every $T$ seconds, they come back to shear the sheep. Upon shearing the sheep, all the sheep are wool-less. By the time they come back for the next shearing, the sheep have regrown some amount of wool. Let's say the number of sheep that have their wool is $N_w$. So when the farmer shears the sheep, they get $N_w$ wool. Then the average rate at which the farmer gathers wool is

$$F_{slow}=\frac{N_w}{T}$$

Now we need to consider the value of $N_w$. A sheep will only regrow wool upon eating if it doesn't already have any, so we should expect that the rate at which sheep are regrowing wool is the rate at which they are eating, $\frac{NRN_g}{A}$, times the fraction of sheep that are wool-less:

$$\dot{N}_w=\frac{dN_w}{dt}=\frac{N-N_w}{N}\frac{NRN_g}{A}=(N-N_w)\frac{RN_g}{A}$$

At time $t=0$, the farmer shears all the sheep, leaving them with $N_w=0$, and then at time $t=T$, the farmer comes back and shears them again, gathering $N_w$ blocks of wool. How many sheep grew their wool back in time $T$ [s]? We can find out by solving the above equation for $N_w$. We'll separate it into $N_w$ terms and non $N_w$ terms:

$$\frac{1}{N-N_w}dN_w=\frac{RN_g}{A}dt$$

and integrate from initial state $(t=0, N_w=0)$ to the final state $(t=T, N_w=N_w(T))$. We're assuming the pasture has reached steady state, so $N_g$ is independent of $t$ and can be treated as a constant.

$$\int_{0}^{N_w(T)}\frac{1}{N-N_w}dw=\int_{0}^{T}\frac{RN_g}{A}dt$$

$$-\ln\left(\frac{N-N_w(T)}{N}\right)=\frac{TRN_g}{A}$$

And solve for $N_w(T)$:

$$N_w(T)=N-N\exp\left(\frac{TRN_g}{A}\right)$$

So when the farmer comes back to shear the sheep at $t=T$, they will get $N-N\exp\left(-\frac{TRN_g}{A}\right)$ blocks of wool. If the farmer keeps coming back every $T$ seconds, they will be gathering wool at a rate

$$F_{slow}=\frac{N-N\exp\left(-\frac{TRN_g}{A}\right)}{T}$$

Finally, we can plug (\ref{grass_cover}) for $N_g$:

\begin{equation}
    \label{fslow}
F_{slow}=\frac{N-N\exp\left(-TR+\frac{TR^2N}{Ak}\right)}{T}
\end{equation}

This is the expression for the rate of wool gathering by a slow farmer. The optimal density of sheep is now dependent on T. To find the optimal sheep population, we can take the same approach as we did with the fast farmer case. Differentiating (\ref{fslow}) w.r.t. $N$:

$$\frac{dF}{dN}=\frac{1-\left(\frac{TR^2N}{Ak}+1\right)\exp\left(-TR+\frac{TR^2N}{Ak}\right)}{T}=0$$

Attempting to solve for $N$ leads to

$$\left(\frac{TR^2N}{Ak}+1\right)\exp\left(\frac{TR^2N}{Ak}+1\right)=e^{TR+1}$$

At this point we must invoke the lambert $W$ function, a nonelementary function satisfying

$$W(x)\exp\left(W(x)\right)=x$$

applying this leads to

$$W(\exp(TR+1))=\frac{TR^2N}{Ak}+1$$

\begin{equation}
    \label{fslow_optimal_n}
N=\frac{Ak}{TR^2}(W(\exp(TR+1))-1) 
\end{equation}

Plugging this back into (\ref{fslow}), we get

$$F_{slow}=\frac{Ak(W(\exp\left(TR+1\right))-1)(1-\exp\left(-TR+W(\exp(TR+1))-1\right))}{R^2T^2}$$

$$F_{slow}=\frac{Ak(W(e^{TR+1})-1)(1-e^{-TR+W(e^{TR+1})-1})}{R^2T^2}$$

\section{Experimental determination of rate constants}

Before proceeding further, it's worth determining the actual values of $k$ and $R$. This will be done via in-game experiments, which will be described briefly here. More detail can be found on the author's github\footnote{github.com/ELFREELAND/minecraft\_sheep}.

Ten sheep were locked in cells with a grass block immediately underneath them. The cells included circuitry to count when each sheep ate the grass and automatically replenish it. In one hour, the sheep consumed 320 blocks of grass. This gives an R value of 0.00888 s\textsuperscript{-1}.

A 30x30 patch of dirt was isolated from its surroundings and uniformly interspersed with 100 grass blocks. As the grass grew, the grass population was recorded every 5 seconds. The data was fit to the exact solution of the logistic equation:

$$y = \frac{p}{1+\left(\frac{p-y_0}{y_0}\right)\exp(-kt)}$$

The best fit gave $k=0.00773.$ The data and fit are compared in Figure \ref{fig:grass_logistic_fit}. The close fit gives us also confidence that the logistic model is appropriate.

\begin{figure}[h]
    \caption{Logistic model fit to grass growth data}
    \label{fig:grass_logistic_fit}
    \includegraphics[width=\textwidth]{grass_growth/grass_logistic}
\end{figure}

\section{Model Discussion}

Figure \ref{fig:ffast} shows $F_{fast}$ and $X_g$ as a function of $N$, normalized for pasture area. These are simple curves,a parabola and straight line respectively, but there are a few important features to glean. First, the equilibrium $X_g$ and $F_{fast}$ are both zero at $\frac{N}{A}=\frac{k}{R}=0.869$. Deterministically, a pasture with this density will lose all of its grass.

Second, the maximum $F_{fast}$ occurs at $X_g=0.5$. This is a feature of the logistic model; population growth is greatest when the population is half of the carrying capacity.

\begin{figure}[h]
    \caption{Plot of $\frac{F_{fast}}{A}$ and $X_g$ as a function of N.}
    \label{fig:ffast}
    \includegraphics[width=\textwidth]{f_fast}
\end{figure}

Figure \ref{fig:fslow} shows $F_{slow}$ as a function of $N$ and $T$. For the case of $T=1$, $F_{slow}$ follows the same parabolic pattern with respect to $N$ as $F_{fast}$. We can see this algebraically by employing the following approximation, for small $x$:

$$e^x-1\approx x$$

Applying this to (\ref{fslow}):

$$F_{slow}\approx NR-\frac{N^2R^2}{Ak}=F_{fast}$$

For large values of T and modest values of N, $F_{slow}$ is approximately linear w.r.t. $N$. Algebraically, the term $e^{-TR(1-\frac{RN}{Ak})}\approx 0$ in (\ref{fslow}) for large negative values of the exponent. Under these conditions,

$$F_{slow}\approx \frac{N\left(1-0\right)}{T}=\frac{N}{T}$$

Physically, this represents conditions for which all of the sheep have regrown their wool by the time the farmer comes back to harvest, so the farmer is simply gathering $N$ blocks of wool with every harvest.

Lines of $\frac{N}{T}$ are plotted over top of the curves in figure \ref{fig:fslow}.

\begin{figure}[h]
    \caption{Plots of $\frac{F_{slow}}{A}$ as a function of N at constant T, and T at constant N.}
    \label{fig:fslow}
    \includegraphics[width=\textwidth]{f_slow_na_t}
\end{figure}

\section{Stochastic simulations}

Thus far we have considered a deterministic model for the grass population of the pasture. This allows for convenient exact solutions for the population evolution, but we could get a more complete picture of the mechanics at work by incorporating the stochastic nature of the pasture into our model.

While stochastic models for populations can be developed without terrible difficulty, they are almost always mathematically intractable (reference). Even the simple logistic model employed here requires simplifying assumptions for theoretical analysis.

Here, we will employ a monte carlo-style numerical approach to model the stochastic behavior of the pasture.

\subsection{The fast farmer}
Within a small time interval $\Delta t$, a sheep has a chance $RX_g\Delta t$ to eat. For a pasture with $N$ sheep, the number of sheep that eat in time $\Delta t$ is a binomial random variable of $N$ trials and $RX_g\Delta t$ success probability.

The law of rare events states that a binomial distribution with a large number of events $n$ and a small probability of success $p$ can be approximated by a poisson distribution with expected value $np$. We can use this to replace the above binomial distribution with a simpler poisson distribution, with expected value $NRX_g\Delta t$:

\begin{equation}
    \Pr(\Delta(AX_g)_{e}=-x)=\frac{(NRX_g\Delta t)^xe^{-NRX_g\Delta t}}{x!}
    \label{sheep_eat_poisson}
\end{equation}


Where $\Delta(AX_g)_{e}$ is the change in grass population due to sheep eating. Note that $\Delta(AX_g)_{e}$ is always negative.

%need to explain why LRE is valid

Similarly, we can model grass growth with a poisson distribution. The probability that the grass population changes by an amount $\Delta(AX_g)_{r}$ in time $\Delta t$ due to grass regrowth is a poisson distribution with expected value $A\dot{X}_g\Delta t$, which is given by (\ref{grass_regrow}):

\begin{equation}
    \Pr(\Delta(AX_g)_{r}=x)=\frac{(kAX_g(1-X_g)\Delta t)^xe^{kAX_g(1-X_g)\Delta t}}{x!}
    \label{grass_regrow_poisson}
\end{equation}

The overall change in $AX_g$ is the sum of the changes due to eating and grass regrowth:

$$\Delta(AX_g) = \Delta(AX_g)_{e} + \Delta(AX_g)_{r}$$

The probability distribution of $\Delta(AX_g)$ is a convolution of these two distributions:

$$\Pr(\Delta(AX_g)=x)=\sum_{n=-\infty}^{\infty}\Pr(\Delta(AX_g)_{e}=-n)\Pr(\Delta(AX_g)_{r}=n+x)$$

\begin{equation}
    =\sum_{n=-\infty}^{\infty}\frac{(NRX_g\Delta t)^ne^{-NRX_g\Delta t}}{n!}\frac{(kAX_g(1-X_g)\Delta t)^{n+x}e^{-kAX_g(1-X_g)\Delta t}}{(n+x)!}
    \label{grass_pop_poisson_conv}
\end{equation}

\subsection{The slow farmer}
The mechanics of sheep eating and grass regrowth are the same as for the fast farmer case, so we'll turn our attention to wool growth. If $n$ sheep eat grass, and the number of sheep that have their wool is $N_w$, each woolless sheep has change $\frac{n}{N}$ to regrow its wool. Therefore the number of sheep that regrow their wool is a binomial random variable with $N-N_w$ trials and $\frac{n}{N}$ probability of success.

\end{document}